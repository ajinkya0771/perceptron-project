[project]
name = "perceptron"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ajinkya Dhote", email = "ajinkyadhote7@gmail.com" }
]
requires-python = ">=3.13"
dependencies = []

[project.scripts]
perceptron = "perceptron:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
